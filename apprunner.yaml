version: 1.0
runtime: nodejs14
build:
  commands:
    - yarn install 
    - composer install --no-dev
    - php artisan key:generate
    - php artisan config:cache
    - php artisan migrate --force
    - yarn run
    - php artisan serve
